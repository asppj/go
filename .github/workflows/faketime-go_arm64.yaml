name: Faketime-Go

on:
  push:
    branches: [ release/* ]
  pull_request:
    branches: [ release/* ]
env:
  GOARCH: arm64
jobs:
  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-20.04 ]
        arch: [ arm64, amd64 ]
    steps:

      - name: Set up Go 1.x
        uses: actions/setup-go@v2
        with:
          go-version: 1.18
        id: go

      - name: Check out code into the Go module directory
        uses: actions/checkout@v2
        with:
          submodules: true
      - name: build by all.bash
        run: |
          export GOROOT_BOOTSTRAP=`pwd`
          export CGO_ENABLE=0
          go env
          ls 
          pwd

          cd src && ./all.bash && cd -
          ls -al
        env:
          GOARCH: ${{ matrix.arch }}
      - name: Build the Docker image
        run: |
          docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
          docker buildx build --platform linux/$GOARCH -t asppj/go-builder:18.6faketime  --file Dockerfile .
          docker push asppj/go-builder:18.6faketime
        env:
          DOCKER_USERNAME: ${{secrets.DOCKER_USER}}
          DOCKER_PASSWORD: ${{secrets.DOCKER_PASS}}
          GOARCH: ${{ matrix.arch }}


      # - name: Create Release
      #   id: create_release
      #   uses: actions/create-release@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
      #   with:
      #     tag_name: ${{ github.ref }}
      #     release_name: Release ${{ github.ref }}
      #     body: |
      #       Changes in this Release
      #       - SetDemFakeTime
      #     draft: false
      #     prerelease: false



